`tmcc` provides a compiler from tMUGEN to CNS.

The tMUGEN syntax is an extension of the CNS syntax. It adds constructs for reusability, reducing code size, and increasing readability.

`tmcc` also performs type checking and verifies inputs at compile time.